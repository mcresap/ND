#include <iostream>

using namespace std;

int main() {
	int legth=0, width=0;
	//read in input - length and width

	cin >> length;
	cin >> width;
	//check that input is collected/works

	//tell user what input was accepted
	// campare length and width

return 0;
