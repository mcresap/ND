#include <iostream>
using namespace std;
#include <vector>
int main(){

    vector<int> v={2, 16, 77, 34, 50};
    cout << "vector contains: "
    for (auto it=v.begin(); it < v.end(); it++)
        cout << *it << " ";
    cout endl;
    cout << "vector size is: " << arr.size();

        v.push_back(29) //pushes 29 to the back of the vector
